pub struct Chunker;

impl Chunker {
    pub fn split_text(_text: &str, _chunk_size: usize, _overlap: usize) -> Vec<String> {
        // Implement simple recursive character splitting or standard token splitting
        let chunks = Vec::new();
        // TODO: use text-splitter crate or manual logic
        chunks
    }
}
